package com.example.cashmanager.ui.login

/**
 * Server details post authentication that is exposed to the UI
 */
data class ServerDataView(
    val displayIp: String
    //... other data fields that may be accessible to the UI
)
